--- a/layouts/rss.xml
+++ b/layouts/rss.xml
@@ -1,3 +1,4 @@
+{{- /* Ventz Changes: theme copy; only change is site.Language.Locale replacing the .LanguageCode accessor deprecated in Hugo 0.158 */}}
 {{- $authorEmail := "" }}
 {{- with site.Params.author }}
   {{- if reflect.IsMap . }}
@@ -45,7 +46,8 @@
     </image>
     {{- end }}
     <generator>Hugo</generator>
-    <language>{{ site.Language.LanguageCode }}</language>{{ with $authorEmail }}
+    {{- /* Ventz Changes: site.Language.Locale replaces .LanguageCode (deprecated in Hugo 0.158) */}}
+    <language>{{ site.Language.Locale }}</language>{{ with $authorEmail }}
     <managingEditor>{{.}}{{ with $authorName }} ({{ . }}){{ end }}</managingEditor>{{ end }}{{ with $authorEmail }}
     <webMaster>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</webMaster>{{ end }}{{ with site.Copyright }}
     <copyright>{{ . | markdownify | plainify | strings.TrimPrefix "© " }}</copyright>{{ end }}{{ if not .Date.IsZero }}
