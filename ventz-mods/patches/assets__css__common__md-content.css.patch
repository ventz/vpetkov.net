--- a/assets/css/common/md-content.css
+++ b/assets/css/common/md-content.css
@@ -8,15 +8,21 @@
 .md-content h1 {
     margin: 40px auto 32px;
     font-size: 40px;
+    /* Ventz Changes: ADDED HR Border under heading */
+    border-bottom: 1px solid var(--tertiary);
 }
 
 .md-content h2 {
     margin: 32px auto 24px;
     font-size: 32px;
+    /* Ventz Changes: ADDED HR Border under heading */
+    border-bottom: 1px solid var(--tertiary);
 }
 
 .md-content h3 {
     font-size: 24px;
+    /* Ventz Changes: ADDED HR Border under heading */
+    border-bottom: 1px solid var(--tertiary);
 }
 
 .md-content h4 {
@@ -34,6 +40,9 @@
 .md-content a:not(.anchor) {
     text-underline-offset: 0.3rem;
     text-decoration: underline;
+    /* Ventz Changes: ADDED RED Color for links */
+    /*color: rgb(210, 53, 62);*/
+    color: var(--accent-link);
 }
 
 .md-content del {
@@ -46,7 +55,15 @@
 .md-content figure:not(:last-child),
 .md-content ul:not(:last-child) {
     margin-bottom: var(--content-gap);
+}
+
+/* Ventz Changes BEGIN: ADDED font-size */
+.md-content p,
+.md-content figure,
+.md-content ul {
+    font-size: 20px;
 }
+/* Ventz Changes END */
 
 .md-content ol,
 .md-content ul {
@@ -251,3 +268,12 @@
     border-radius: var(--radius);
     max-width: 100%;
 }
+
+/* Ventz Changes (a11y) BEGIN: copy-code is hover-only in the theme (display:none removes it
+   from the tab order); reveal it for keyboard focus too */
+div.highlight:focus-within .copy-code,
+pre:focus-within .copy-code,
+.copy-code:focus-visible {
+    display: block;
+}
+/* Ventz Changes END */
