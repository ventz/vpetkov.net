--- a/assets/css/core/theme-vars.css
+++ b/assets/css/core/theme-vars.css
@@ -1,8 +1,12 @@
 :root {
-    --gap: 24px;
+    /* Ventz Changes: Made Gap Bigger: 24px; */
+    --gap: 34px;
+    /* Ventz Changes: ADDED a Gap between posts */
+    --post-gap: 80px;
     --content-gap: 20px;
     --nav-width: 1024px;
-    --main-width: 720px;
+    /* Ventz Changes: Made Pager Wider: 720px; */
+    --main-width: 820px;
     --header-height: 60px;
     --footer-height: 60px;
     --radius: 8px;
@@ -15,6 +19,8 @@
     --code-block-bg: rgb(28, 29, 33);
     --code-bg: rgb(245, 245, 245);
     --border: rgb(238, 238, 238);
+    /* Ventz Changes (a11y): single knob for the red accent links (7.9:1 on white) */
+    --accent-link: rgb(155, 34, 44);
     color-scheme: light;
 }
 
@@ -22,15 +28,27 @@
     --theme: rgb(29, 30, 32);
     --entry: rgb(46, 46, 51);
     --primary: rgb(218, 218, 219);
-    --secondary: rgb(155, 156, 157);
+    /* Ventz Changes (a11y): was rgb(155,156,157) — 4.33:1 on --code-bg; now 4.9:1 */
+    --secondary: rgb(166, 167, 168);
     --tertiary: rgb(65, 66, 68);
     --content: rgb(196, 196, 197);
-    --code-block-bg: rgb(46, 46, 51);
+    /* Ventz Changes (a11y): near-black keeps Chroma tokens >= 4.5:1 on post pages;
+       the lighter list-preview background moved to .entry-content .highlight below */
+    --code-block-bg: rgb(38, 39, 44);
     --code-bg: rgb(55, 56, 62);
     --border: rgb(51, 51, 51);
+    /* Ventz Changes (a11y): dark-mode red was 2.1:1 on the page background; 5.1:1 now */
+    --accent-link: rgb(224, 108, 117);
     color-scheme: dark;
 }
 
+/* Ventz Changes BEGIN: keep the lighter "Dark Visible in list mode" background for previews
+   (a11y: --code-bg instead of --tertiary so Chroma tokens stay >= 4.5:1) */
+[data-theme="dark"] .entry-content .highlight {
+    background: var(--code-bg) !important;
+}
+/* Ventz Changes END */
+
 .list {
     background: var(--code-bg);
 }
