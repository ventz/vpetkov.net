--- a/assets/css/core/reset.css
+++ b/assets/css/core/reset.css
@@ -25,7 +25,8 @@
 
 body {
     font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
-    font-size: 18px;
+    /* Ventz Changes: Made BOLD Text Smaller: 18px; */
+    font-size: 16px;
     line-height: 1.6;
     word-break: break-word;
     background: var(--theme);
@@ -116,3 +117,25 @@
     display: block;
     max-width: 100%;
 }
+
+/* Ventz Changes (a11y) BEGIN: visible keyboard focus for the search box (reset removes outlines) */
+input:focus-visible,
+textarea:focus-visible {
+    outline: 2px solid var(--primary);
+    outline-offset: 2px;
+}
+/* Ventz Changes END */
+
+/* Ventz Changes (a11y) BEGIN: visually-hidden-but-announced utility (clip method, not zero-size) */
+.sr-only {
+    position: absolute;
+    width: 1px;
+    height: 1px;
+    padding: 0;
+    margin: -1px;
+    overflow: hidden;
+    clip: rect(0, 0, 0, 0);
+    white-space: nowrap;
+    border: 0;
+}
+/* Ventz Changes END */
